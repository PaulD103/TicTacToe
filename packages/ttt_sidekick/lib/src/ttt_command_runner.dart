import 'package:args/command_runner.dart';

class TttCommandRunner extends CommandRunner {
  TttCommandRunner()
      : super('Sidekick', 'A convenience script that simplifies setting up and running your project project.');
}
